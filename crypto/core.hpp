#ifndef CORE_H_
#define CORE_H_

#include <string>

template <constexpr std::string k> class Z {
public:
  Zmod() : _value("hej");
  Zmod(string value) : _value(value);
  virtual ~Zmod();
  string _value;
  int len() { return _len }

private:
  const int _len = 4 * k.length() + 1 bool bits[len]
};

#endif // CORE_H_
